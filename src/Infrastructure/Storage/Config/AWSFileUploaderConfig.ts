// import { FileDTO } from "../../../Domain/DTO/FileDTO";
// import { AWSFileUploaderConfigDTO } from "../../DTO/AWSFileUploaderConfigDTO";
// import moment from "moment";

export class AWSFileUploaderConfig {
  //TODO:
  // constructor(args: { file: FileDTO }) {}
  //
  // getConfig(): AWSFileUploaderConfigDTO {
  //   return this.generateFileName({
  //       file: input.file,
  //       timestamp: _.toNumber(moment().utc(false).format("x")),
  //   });
  // }
  //
  // private generateFileName(args: { file: FileDTO; timestamp: number }): string {
  //   return `${args.file.name}-${args.timestamp}.${args.file.extension}`;
  // }
}
